global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'fiap-srv-user'
    metrics_path: /metrics
    static_configs:
      # 'fiap-srv-games' deve ser o nome do serviço no docker-compose
      # ou 'localhost' se estiver rodando fora do Docker
      - targets: ['fiap-srv-user:8080']